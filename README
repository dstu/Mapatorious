Building is with Maven. To do the build, you will have to
install the Clojure and Clojure-contrib jars into your Maven
repository. (Our project depends on both of these, and right now
I don't know of a repository where we can get them from
automatically.)

Run these commands:

mvn install:install-file -Dfile=/path/to/clojure-contrib-1.1.0.jar -DgroupId=org.clojure -DartifactId=clojure-contrib -Dversion=1.0-SNAPSHOT -Dpackaging=jar

mvn install:install-file -Dfile=/path/to/clojure-1.1.0.jar -DgroupId=org.clojure -DartifactId=clojure-lang -Dversion=1.1.0-SNAPSHOT -Dpackaging=jar

Then, you can compile with:

mvn clojure:compile

You can run with:

mvn clojure:run
